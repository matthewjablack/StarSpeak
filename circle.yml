dependencies:
  override:
    - bundle install: # note the colon here
        timeout: 240
    - npm i -g yarn:
        timeout: 240
    - cd client && yarn install:
        timeout: 240

deployment:
  production:
    branch: master
    commands:
      - bundle exec cap production deploy