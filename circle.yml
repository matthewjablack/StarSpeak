# dependencies:
#   override:
#     - bundle install: # note the colon here
#         timeout: 240
#     - yarn postinstall:
#         timeout: 240

deployment:
  production:
    branch: master
    commands:
      - bundle exec cap production deploy

machine:
  node:
    version: 6.10.2
