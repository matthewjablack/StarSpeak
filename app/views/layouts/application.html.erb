<!DOCTYPE html>
<html lang="en" >
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Helping students to embrace their presentation. 
      Using speech to text technology, StarSpeak monitors your speech to determine your 
      language tone and confidence level.">
    <meta name="author" content="">
    <meta name="google-site-verification" content="jFQ2e2MxeYBWwIBsrckjkJaeMS8t_3ST3uTUNRn72ik" />
    <link rel="shortcut icon" href="../../assets/ico/favicon.ico">

    <title>StarSpeak</title>

    <script src="https://download.affectiva.com/js/3.2/affdex.js"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/wavesurfer.js/1.2.3/wavesurfer.min.js"></script>

    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= favicon_link_tag 'favicon.ico' %>

    <!-- Custom styles for this template -->
    <!--[if lt IE 9]><script src="../../assets/js/ie8-responsive-file-warning.js"></script><![endif]-->

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->

    <%= csrf_meta_tags %>

    <% if Rails.env.production? %>
      <script type="text/javascript">
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');
        ga('create', 'UA-101621947-1', 'auto');
      </script>
    <% end %>
  </head>
  <body class="light-theme <%= get_body_class %>" <%= yield(:body_attributes) %>>

    <%= render 'shared/header' %>

      <% if (params[:controller] == "lessons" && params[:action] == "show") || (params[:controller] == "hello_world") %>
          <%= yield %>
      <% else %>
          <%= yield %>
      <% end %>

    <% if params[:controller] == "home" && (params[:action] == "index" || params[:action] == "appleby")%>
      <%= render :partial => "shared/footer" %>
    <% end %>
    
    <% unless flash.nil? %>
      <% flash.each do |key, value| %>
        <script>
          alert_with_toast("<%= value %>");
        </script>
      <% end %>
    <% end %>


    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
  
  </body>
</html>
